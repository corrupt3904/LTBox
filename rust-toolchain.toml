# Pin the toolchain so local tests/Clippy and CI use the same compiler.
# CI installs this version through rustup; update the pin with CI checks.
[toolchain]
channel = "1.95.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
